# Secure Storage

This directory is used for storing sensitive files and configurations used by the application, such as private keys, API keys, and other secrets.

**This directory and its contents should not be committed to version control.** It is automatically created by the application when needed.

If you have cloned this repository, this directory might be empty. The application will generate the necessary files and configurations as you use it.
